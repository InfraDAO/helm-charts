{{- if .Values.authrpc.jwtSecret.value }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.authrpc.jwtSecret.secretName }}
stringData:
  {{ .Values.authrpc.jwtSecret.key }}: {{ .Values.authrpc.jwtSecret.value }}
{{- end }}
